import 'package:flutter/widgets.dart';

class PlaceOfBirth extends StatelessWidget {
  const PlaceOfBirth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
