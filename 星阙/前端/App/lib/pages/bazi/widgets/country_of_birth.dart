import 'package:flutter/widgets.dart';

class CountryOfBirth extends StatelessWidget {
  const CountryOfBirth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
