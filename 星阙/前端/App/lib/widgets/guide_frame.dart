import 'package:flutter/material.dart';

class GuideFrame extends StatelessWidget {
  const GuideFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
